---
title: 轮廓测量 (ContourMeasure)
---

## 字段 (Fields)

### `next`

返回路径中的下一个 [轮廓测量 (ContourMeasure)](/scripting/api-reference/contour-measure) 对象，如果这是最后一个轮廓，则返回 nil。在调用 `path:contours()` 之后，可以使用此字段遍历路径中的所有轮廓。
