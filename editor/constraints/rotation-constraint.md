约束

# 旋转约束 (Rotation Constraint)

旋转约束让受约束对象跟随目标对象的旋转角度。

## [​](#properties) 属性

- **偏移 (Offset)**：在跟随旋转的基础上增加一个固定角度偏差。
- **强度 (Strength)**：跟随旋转的紧密程度。

[位移约束 (Translation Constraint)](/editor/constraints/translation-constraint.md)[缩放约束 (Scale Constraint)](/editor/constraints/scale-constraint.md)