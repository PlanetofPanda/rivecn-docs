# 第二阶段任务：彻底解决文档死链 (404) 与 SEO 跳出率优化

## 背景与问题描述
在文档的汉化与搬运过程中，由于部分原始 Markdown 页面未被完全克隆，或者链接格式包含相对路径与无效的外部协议头（例如 `](https://...)`），导致 VitePress 内部检测出了 99 处失效链接。
死链不仅导致构建失败，在 SEO 中更是大忌，会导致爬虫直接丢弃索引并严重影响页面权重。

## 解决策略（全自动降级方案）
由于缺失页面无法主观凭空翻译，最好的方案是：**保留这些超链接，但统一导向 Rive 官方的英文文档对应页面 (`https://help.rive.app`)**。
这样不仅消除了 404 死链，也保证了用户依然能跳转获取信息。

## 任务拆解清单

- [ ] **1. 分析构建日志**
  - 解析刚才 `npm run docs:build` 生成的含有 99 条警告的脱机日志文件。
- [ ] **2. 编写智能化正则匹配 Python 脚本 (`tools/fix_dead_links.py`)**
  - 读取日志提取出错信息 `[死链名] -> [所在文件]`。
  - 打开对应文件，寻找存在问题的 markdown link。
  - 对于 `/https://...` 这种异常结构，切除头部的 `/`。
  - 对于其他的本地内部死链（如 `/editor/animate-mode/...`），统一补全前缀，降级替换为 `https://help.rive.app/...`。
- [ ] **3. 执行替换并验证**
  - 运行 Python 脚本完成百余个链接的批量精准提换。
  - 再次跑 `npm run docs:build`，验证原本的 99 个 `(!)` 警告是否完全消失！
- [ ] **4. 收尾工作**
  - 开启 `config.mts` 的 `ignoreDeadLinks: false`（移除忽略），确保未来更新文章时能第一时刻在 CI 发现新的未汉化死链！

> 本清单文件由 Antigravity 生成，用于指引 404 修复流程。
